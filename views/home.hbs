{{>header}}

<div class="container">
	<div class="row">
		<div class="col-sm-8 offset-sm-2">
			<form action="/" method="POST">
				<input type="number" name="paragraphs" placeholder="Number of Paragraphs" required="required">
				<button type="submit" class="btn btn-primary">Ahoy!</button>
			</form>
			{{#each lorem}}
				<p>{{this}}</p>
			{{/each}}
		</div>
	</div>
</div>

{{>footer}}